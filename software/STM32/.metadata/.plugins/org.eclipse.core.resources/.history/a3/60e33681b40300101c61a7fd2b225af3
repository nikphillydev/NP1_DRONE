/*
 * PMW3901.cpp
 *
 *  Created on: Mar 17, 2025
 *      Author: Nikolai Philipenko
 */

#include "Drivers/PMW3901.hpp"

PMW3901::PMW3901(SPI_HandleTypeDef& spi_handle, osMutexId_t& spi_mutex, GPIO_TypeDef* cs_port, uint16_t cs_pin)
	: spi_handle(spi_handle),
	  spi_mutex(spi_mutex),
	  cs_port(cs_port),
	  cs_pin(cs_pin) {}

bool init()
{

}

/*
 *
 *  Low-level register read / write
 *
 */
bool read_register(uint8_t reg_addr, uint8_t* rx_data, uint16_t data_len)
{
	bool status = false;
	return status;
}

bool write_register(uint8_t reg_addr, uint8_t* tx_data, uint16_t data_len)
{
	bool status = false;
	return status;
}

